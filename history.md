## 2026-08-17 — 앱 법적 본문의 GitHub Pages 단일 출처 정리

### 결정

- 이용자에게 표시하는 법적 본문은 기존 GitHub Pages canonical 문서 4종과 12개 slug 매핑만 사용한다.
- 과거 개인정보·이용약관·아동안전 설정 딥링크는 빈 화면이나 중계 컴포넌트를 렌더하지 않고 해당 canonical 내부 라우트로 redirect한다.
- 참조되지 않는 앱 번들 법적 txt와 LegacyHost는 제거하되, 프로젝트 루트 `legal/`은 외부 공개 저장소에 반영하기 전 검토 초안으로 계속 보존한다.

### 이유

- 앱 번들의 오래된 본문과 공개 문서가 서로 달라지는 경로를 없애면서 기존 딥링크 호환성과 공개 전 검토 자료의 경계를 유지하기 위함이다.

## 2026-08-17 — 프로필 사진 신규 업로드를 2장으로 제한

### 논의

- 사용자는 당분간 프로필 사진을 최대 2장까지만 등록하게 하고, 서비스 운영 상황에 따라 나중에 허용 수를 확대하기로 했다.
- 화면에서만 추가를 막으면 별도 사진 관리 경로나 직접 API·동시 요청으로 제한을 우회할 수 있었다.

### 결정

- 프로필과 설정의 사진 관리 화면은 1·2번 빈 슬롯에서만 신규 추가를 허용하고 나머지 빈 슬롯은 잠금 상태로 표시한다.
- 서버도 처리 전 개수 검사와 최종 DB 갱신 조건을 함께 적용하여 일반 요청과 동시 요청 모두 신규 업로드가 2장을 넘지 못하게 한다.
- 이미 3장 이상 저장된 계정의 사진은 임의로 삭제하지 않고 계속 표시·관리·삭제할 수 있게 하며, 신규 업로드만 제한한다.

### 이유

- 모든 진입 경로에서 같은 운영 정책을 보장하면서, 기존 사용자의 사진을 예고 없이 삭제해 데이터 손실을 일으키지 않기 위함이다.

## 2026-08-17 — 선택 동의를 실제 기능 사용 시점에 연동

### 논의

- 가입 화면에서 민감정보·연락처 선택 동의를 받아도 실제 프로필 성향 저장과 연락처 지인 제외 기능이 현재 활성 문서 버전의 동의 상태를 서버에서 강제하지 않으면 미동의·구버전 동의로도 선택 기능이 작동할 수 있었다.
- 민감정보 선택 동의의 대상을 자유문이나 일반 프로필 전체로 확대하지 않고, 실제 성향 기능에 필요한 구조화 필드로 한정할 필요가 있었다.

### 결정

- 연락처 지인 제외 기능은 `contacts-consent` 현재 활성 버전에 동의한 경우에만 해시 저장과 ON을 허용하고, 기능 사용 시점에 안내·동의를 제공한다. 철회·배치 미선택은 해시 삭제와 OFF를 동반한다.
- 민감정보 선택 동의 대상은 `preference`와 `search_preference`로만 한정하고, `sensitive-information-consent` 현재 활성 버전 동의 후에만 두 성향을 저장한다. 철회·배치 미선택은 두 필드를 비우며, 신규 기본값도 빈 값으로 유지한다.
- `selfintro` 자유문, 일반 성별, 결혼 여부와 기타 프로필·검색 설정은 민감정보 선택 동의 대상으로 확대하지 않는다.

### 이유

- 선택 동의 거부가 기본 서비스 이용을 막지 않도록 하면서, 선택 기능은 현재 문서 버전의 명시적 동의가 있을 때만 사용하고 철회 후 잔존 데이터를 남기지 않기 위함이다.

### 다음에 이어갈 내용

- 별도 `tazocode-legal` 저장소에 정리된 공개 법적 문서와 `privacy.html#sensitive` 앵커를 반영·배포한다.
- 실제 Android/iOS 기기에서 연락처 권한·동의·철회·ON/OFF와 두 성향의 동의·편집·철회 흐름을 확인한다.

## 2026-08-17 — 모바일 법적 문서 표시와 가입 동의 메타데이터 활성화

### 논의

- 설정의 개인정보 처리방침 표는 공개 원문에서 최소 680px 가로 스크롤 방식이었고, 앱의 cross-origin iframe 안에서는 중첩 스크롤이 잘린 화면처럼 보이며 조작 방법도 명확하지 않았다.
- 로컬 개발 DB의 활성 약관 메타데이터가 0개여서 서버가 동의할 항목이 없는 상태를 완료로 오인하고 가입 동의가 진행되지 않았다.
- 기존 약관 시드는 현재 공개 문서·필수/선택 정책과 다르고 실행일마다 새 버전을 만들어 불필요한 재동의를 발생시킬 수 있었다.

### 결정

- 모바일 법적 문서 화면에 표를 좌우로 밀어 확인하는 안내와 동일한 allowlist 공개 원문을 WebView 전체 화면에서 여는 기능을 제공한다. GitHub Pages 원문과 기존 iframe 보안 정책은 유지한다.
- 가입 동의 메타데이터는 공개 문서 시행일에 맞춘 고정 버전 `2026-08-13-01`로 관리하며, 필수는 이용약관·커뮤니티 안전·아동 안전·개인정보 수집 이용 4개, 선택은 민감정보·연락처 지인 제외 2개로 구성한다.
- 시드는 같은 slug와 버전을 upsert하고 다른 활성 버전만 비활성화하여 반복 실행해도 중복이나 불필요한 재동의를 만들지 않는다. 서버 시작 시 자동 실행하지 않고 대상 환경을 명시해 운영자가 실행한다.
- 필수 메타데이터 누락은 설정 오류로 차단하고, 필수 항목을 누락한 배치 동의 요청은 저장 전에 거부한다. 선택 항목 미동의는 `optedIn:false`로 기록하고 서비스 진행을 막지 않는다.
- 로컬 개발 DB에는 메타데이터 6개를 적용했으며 운영 DB는 배포 전 별도 명령으로 적용한다.

### 이유

- 모바일에서 공개 법적 문서의 표를 읽을 수 있게 하면서 법적 본문의 단일 공개 출처를 유지하고, DB 설정 누락이나 클라이언트 우회로 필수 동의를 건너뛰지 못하게 하기 위함이다.

### 다음에 이어갈 내용

- 운영 배포 전 `NODE_ENV=production npm run seed:terms`를 실행하고 일반 계정으로 동의 4필수·2선택, 나이, 성별, 메인 진입 전체 흐름을 실제 기기에서 확인한다.

## 2026-08-17 — 약관 동의 목록과 공개 법적 문서 연결 명확화

### 논의

- 밝은 화면 테마에서 약관 항목명이 사실상 보이지 않아 필수·선택 표지만 반복되어 보였고, 이용자가 어떤 항목에 동의하는지와 세부 내용을 확인하기 어려웠다.
- 동의 필요 여부와 버전은 서버가 관리하면서 앱의 항목명·요약·상세 보기는 기존 공개 법적 문서의 단일 출처와 일치시킬 필요가 있었다.

### 결정

- 약관 동의 화면은 서버의 대기 항목·필수 여부·버전을 기준으로 유지하고, 알려진 문서 식별자는 공개 법적 문서 매핑의 항목명·요약·상세 링크를 표시한다.
- 각 항목에 필수 또는 선택 배지와 `자세히 보기`를 제공한다. `모두 동의`는 전체 항목을 선택하지만 제출 조건은 필수 항목 동의만으로 유지한다.
- 매핑이 없는 문서는 정리된 서버 제목 또는 식별자를 표시하되 연결되지 않은 상세 화면은 열지 않고 안내한다.
- 상세 문서를 확인하고 돌아올 때 선택 상태를 임시 보존하되 현재 사용자 ID별로 격리하여 다른 계정에 복원되지 않도록 한다.

### 이유

- 이용자가 동의 대상을 명확하게 이해하고 법적 세부 내용을 같은 공개 출처에서 확인하게 하면서, 선택 동의를 서비스 이용 조건으로 강제하지 않고 계정 간 상태 혼선을 방지하기 위함이다.

### 다음에 이어갈 내용

- GitHub Pages 목표 경로에 공개 HTML 4종을 게시한 뒤 실제 웹·Android/iOS에서 동의 목록과 상세 문서 표시를 확인한다.

## 2026-08-17 — 사용자 표시명을 손끝으로 통일

### 논의

- 설치 아이콘 이름과 로그인 화면은 `손끝`이었지만 Android 액티비티, 웹/PWA 제목, 관리자 로그인, 온보딩, 문의·신고·메일·푸시 일부에는 `TZChat`이 남아 있었다.
- 사용자 브랜드와 법적 공식 서비스명 및 외부 연동에 사용하는 기술 식별자의 변경 범위를 구분할 필요가 있었다.

### 결정

- Android/iOS/Capacitor 설치 표시명, Android 액티비티, 웹 탭·PWA, 로그인·관리자 로그인·온보딩·앱 헤더와 사용자 문의·신고·메일·푸시의 표시명은 `손끝`으로 통일한다.
- 법적 문서의 공식 서비스명 `TZChat (앱 표시명: 손끝)`과 package/application/bundle ID, Firebase 설정, 도메인, URL scheme·딥링크, 알림 채널 ID, 저장소·환경 키 등 기술 식별자는 유지한다.

### 이유

- 이용자에게 하나의 일관된 앱 이름을 제공하면서 기존 설치·푸시·인증·딥링크·도메인 연동을 깨뜨리지 않기 위함이다.

### 다음에 이어갈 내용

- 실제 Android/iOS 설치 화면과 실제 이메일·푸시 발송에서 `손끝` 표시를 확인한다.

## 2026-08-17 — Android 하드웨어 뒤로가기 정책 통합

### 논의

- Android 하드웨어 뒤로가기 리스너가 앱의 두 위치에서 중복 등록되어 한 번의 입력으로 여러 단계 이동하거나 즉시 종료될 수 있었다.
- 앱 내부 이동 이력, 이력 없는 딥링크, 기준 홈, 로그인·필수 약관·온보딩 화면의 동작을 구분할 필요가 있었다.

### 결정

- Android에서만 앱 전역 뒤로가기 리스너 하나를 등록하고 오버레이와 입력 키보드를 먼저 닫는다.
- 내부 이동 이력이 있으면 한 단계만 뒤로가고, 이력 없이 상세 화면으로 진입한 경우 `/home/6page`로 이동한다.
- `/home`, `/home/`, `/home/6page`에서는 첫 입력에 종료 안내를 표시하고 2초 안에 두 번째로 누른 경우에만 앱을 종료해 기기 홈으로 이동한다.
- 로그인, 관리자 로그인, 필수 약관, 온보딩, 탈퇴 유예 화면은 내부 이력이 없을 때 메인 화면으로 우회시키지 않는다.

### 이유

- Android 앱의 일반적인 뒤로가기 경험을 제공하면서 중복 이동과 실수로 인한 즉시 종료, 필수 가입 절차 우회를 방지하기 위함이다.

### 다음에 이어갈 내용

- 실제 Android 기기에서 오버레이·키보드·탭과 상세 화면 이동·딥링크·홈 2회 입력 종료를 확인한다.

## 2026-08-17 — 프로젝트 관리자 중심의 순차 작업 방식

### 논의

- 여러 코드 작업을 진행할 때 작업 책임과 검토 단계를 명확히 하고, 같은 파일을 동시에 수정해 충돌하거나 검증이 누락되는 일을 방지할 필요가 있었다.

### 결정

- Codex는 프로젝트 관리자를 맡고 코드 수정은 하위 프로그래머에게 한 작업씩 순차적으로 지시한다.
- 각 작업 완료 후 Codex가 변경 내용과 영향 범위를 직접 검토하고 관련 테스트를 통과시킨 뒤 다음 작업을 지시한다.
- 여러 하위 프로그래머가 같은 파일을 동시에 수정하지 않도록 작업 범위와 순서를 관리하며, 전체 작업이 끝나면 결과를 종합해 보고한다.

### 이유

- 작업 간 충돌을 줄이고 각 단계의 품질과 책임을 명확히 유지하기 위함이다.

## 2026-08-15 — 환경별 완결 설정으로 전환

### 논의

- 공통 `.env`와 환경별 파일을 함께 읽으면 서버에 일부 파일만 반영되거나 PM2의 과거 환경값이 남을 때 로컬·운영 설정 차이를 파악하기 어려웠다.
- 프론트와 백엔드가 로컬·서버에서 각각 어떤 파일 하나를 기준으로 실행되는지 명확히 하고, 수동 배포 파일 수도 고정할 필요가 있었다.

### 결정

- 2026-07-29의 백엔드 공통 `.env` 자격증명 관리와 2026-07-30의 운영 `.env` 다음 `.env.production` 로딩 결정을 변경한다.
- 프론트·백엔드 모두 공통 `.env`를 사용하지 않고, 로컬은 각 프로젝트의 `.env.development`, 서버 실행과 production 빌드는 각 프로젝트의 `.env.production` 하나를 완결 설정으로 사용한다.
- 백엔드는 `NODE_ENV`에 해당하는 `.env.{environment}` 하나만 필수로 읽으며 shell·PM2 외부값을 파일보다 우선한다. PM2 ecosystem은 production 실행 표식만 제공하고 환경값을 중복 보관하지 않는다.
- 서버 수동 업로드 대상은 `tzchatapp/.env.production`과 `tzchatback/.env.production`이며 실제 환경 파일은 Git에 커밋하지 않는다. 저장소에는 환경별 example만 유지하고 모호한 `.env.example`은 제거한다.

### 이유

- 각 실행 환경의 설정 출처를 한 파일로 제한하여 누락·덮어쓰기·오래된 PM2 환경값으로 인한 로컬과 서버의 동작 차이를 줄이고, 1인 운영의 배포 절차를 단순화하기 위함이다.

### 다음에 이어갈 내용

- 운영 서버에 두 production 환경 파일을 수동 반영한다. 기존 PM2 프로세스는 과거 환경을 유지할 수 있으므로 전환 시 한 번 삭제하고 ecosystem 설정으로 재생성한 뒤 health와 외부 연결을 확인한다.
- 현재 검증은 백엔드 테스트 107/107과 build, 프론트 테스트 41/41과 `build:app`까지 통과했다.

## 2026-08-15 — 배포 안전 기준 강화

### 논의

- 로컬 개발에서는 동작하지만 production 빌드·서버 웹·Capacitor 앱에서 API 주소, 복사된 자산, 런타임 환경 차이 때문에 다른 결과가 생길 수 있어 환경별 배포 경계를 다시 점검했다.
- 외부 실제 점검에서 운영 도메인의 인증서 hostname mismatch가 발생했고, 인증서 검증을 우회하면 웹 루트·API health·Socket.IO polling이 모두 TZChat이 아닌 TAZOCODE HTML을 반환했다.

### 결정

- 프론트 `npm run build`는 production `VITE_API_BASE_URL`이 지정 운영 API Origin과 정확히 일치하는지 빌드 전에 검증한다. `npm run build:app`은 production 빌드 후 Android/iOS에 자산을 복사하고 웹 `dist`와 네이티브 public 자산이 일치하는지 검사한다.
- 프론트 환경 키는 `VITE_API_BASE_URL`, `VITE_APP_VERSION`, `VITE_HTTP_DEBUG`, `VITE_NOTIFICATION_DEBUG`로 정리한다.
- 백엔드는 Node 20 이상을 요구하고 공개 미디어 URL은 `PUBLIC_API_ORIGIN`만 기준으로 생성한다. 폐기된 공개 URL 키는 거부하며 production 런타임에는 저장소 외부의 읽을 수 있는 FCM 서비스 계정 파일을 필수로 검사한다. 백엔드 `npm run build`는 컴파일 산출물이 아니라 구문·환경·배포 조건을 확인하는 배포 전 검사로 유지한다.
- 운영 Nginx 기준본은 최상위 `nginx.md`만 사용하고, 활성 vhost·중복 server_name·인증서 SAN·구문과 reload 및 웹/API/Socket.IO 응답을 각각 확인하는 절차를 따른다. 오래된 백엔드 하위 Nginx 파일은 사용하지 않는다.

### 이유

- 개발 서버의 자동 환경 처리나 과거 네이티브 자산이 production 설정 오류를 숨기지 못하게 하고, 잘못된 vhost·인증서·공개 URL·푸시 자격증명을 배포 전에 발견하기 위함이다.

### 다음에 이어갈 내용

- 운영 서버에 최상위 Nginx 기준본과 올바른 인증서를 반영한 뒤 웹 루트는 TZChat HTML, API health는 JSON, Socket.IO polling은 Engine.IO open packet을 반환하는지 정상 TLS 검증으로 다시 확인한다.
- 최종 전체 검증에서 백엔드 테스트 106개와 프론트 테스트 41개가 통과했고, 백엔드 build 및 프론트 `build:app`의 Android/iOS 자산 복사·일치 검사까지 통과했다.
- 실제 서버 배포와 Android/iOS 실기기 동작은 계속 별도로 검증한다.

## 2026-08-15 — 필수 완료 게이트 강화

### 논의

- 로그인 직후에만 온보딩 단계를 확인하면 새로고침, 앱 재실행, 직접 URL 접근 또는 세션 유지 중 필수 약관 변경으로 미완료 사용자가 메인 화면이나 API에 접근할 수 있었다.
- 필수 약관과 성인 조건은 클라이언트 화면뿐 아니라 서버 접근 제어에서도 같은 기준을 사용해야 했다.

### 결정

- 일반 사용자는 현재 필수 약관 동의, 서버가 유효성과 성인 조건을 확인한 나이 정보, 성별 선택을 모두 완료해야 메인 화면과 보호 API를 이용할 수 있다.
- 로그인 직후뿐 아니라 인증 상태 복구, 새로고침, 앱 재실행, 직접 URL 접근과 세션 중 필수 정책 변경에서도 완료 상태를 다시 조회하여 필요한 단계로 안내한다.
- 운영·지원 목적의 기존 master 예외는 유지하고 선택 약관 거부는 서비스 접근을 막지 않는다.

### 이유

- 화면 진입 경로와 세션 시점에 관계없이 일반 사용자에게 같은 필수 동의·성인 이용 조건을 적용하고 클라이언트 우회로 보호 API에 접근하지 못하게 하기 위함이다.

### 다음에 이어갈 내용

- 실제 브라우저와 Android/iOS 앱에서 새 계정, 단계별 미완료 계정, 정책 변경 후 기존 세션, master 계정의 안내·차단·예외 흐름을 확인한다.

## 2026-08-15 — master 전용 관리자 시각 테스트 페이지

### 논의

- 운영 관리자 화면의 카드, 상태, 표, 버튼과 모바일 배치를 실제 운영 API나 데이터를 변경하지 않고 한 화면에서 비교할 수 있는 전용 시각 샘플이 필요했다.
- 일반 사용자에게 테스트용 진입점이나 운영 샘플을 노출해서는 안 됐다.

### 결정

- master 계정의 하단 메뉴에만 테스트 탭을 표시하고 `/home/admin-test`에 순수 시각 샘플 대시보드를 제공한다.
- 페이지의 수치·상태·버튼은 UI 확인용 정적 샘플이며 실제 API를 호출하거나 서버·DB 데이터를 변경하지 않는다.

### 이유

- 운영 데이터에 영향을 주지 않으면서 화면 구성과 반응형 상태를 빠르게 검토하고, 테스트 도구의 노출 범위를 master로 제한하기 위함이다.

### 다음에 이어갈 내용

- 실제 브라우저와 앱에서 master·일반 계정의 탭 노출 차이, 직접 URL 차단, 모바일·넓은 화면의 레이아웃을 확인한다.

## 2026-08-14 — FCM 운영 자격증명 경계와 네이티브 준비 완료

### 논의

- Apple APNs 키는 Firebase의 TZChat iOS 앱에 개발용·프로덕션용 모두 등록했고, Firebase Admin 서비스 계정은 서버 저장소 밖 `/home/tazofarm/project/firebase/tazocode-firebase-admin.json`에 두기로 했다.
- 운영 `.env.production`은 사용자가 직접 관리하며 코드 작업에서 환경 파일이나 서비스 계정 내용을 읽거나 수정하지 않기로 했다.

### 결정

- 백엔드는 `FCM_SA_PATH`만 사용하고, 설정된 값이 절대경로이며 백엔드 저장소 외부를 가리키는지 DB 연결 전에 검증한다. 존재하는 경로는 실제 경로까지 확인하여 외부 심볼릭 링크가 저장소 내부 파일을 가리키는 경우도 서버 시작 시 거부한다.
- iOS에는 Push Notifications capability와 함께 Background Modes의 Remote notifications를 명시하고, 기존 Firebase Messaging·Capacitor 토큰 및 수신·클릭 구조를 유지한다.
- 실제 `.env*`, 서비스 계정 파일, 배포 상태는 코드 작업에서 변경하지 않는다. 직접 APNs 발송 코드와 `APNS_*` 검증은 iPhone 실기기 FCM 수신이 확인될 때까지 보존한다.

### 다음에 이어갈 내용

- 사용자가 운영 `.env.production`에 `FCM_SA_PATH`를 반영하고 PM2 환경을 재적용한 뒤 Android/iPhone 실기기에서 전체 수신·갱신·해제·라우팅을 검증한다.
- iPhone FCM 검증 완료 후 현재 발송 경로에서 사용하지 않는 직접 APNs 코드와 `APNS_*` 환경 검증을 제거한다.

## 2026-08-14 — Android·iOS FCM registration token 통일

### 논의

- Capacitor Push Notifications의 iOS registration 이벤트는 APNs device token을 반환하지만, 서버의 주 발송기는 Firebase Admin FCM이어서 Android와 iOS의 토큰·발송 경로가 달랐다.
- iOS를 FCM으로 전환할 때 DeviceToken에 transport 필드를 추가하거나 기존 APNs 토큰 마이그레이션을 둘 필요가 있는지, 직접 APNs 발송 코드를 즉시 제거할 수 있는지 검토했다.

### 결정

- 공식 Capacitor Push Notifications의 권한·수신·클릭 흐름은 Android/iOS 모두 유지한다. Android는 기존 registration FCM 토큰을 사용하고, iOS는 Firebase Core/Messaging과 최소 로컬 Capacitor 브리지로 FCM 토큰 최초 발급·갱신·삭제를 처리한다.
- 서버는 Android/iOS를 Firebase Admin FCM으로 통합 발송하고 서비스 계정은 `FCM_SA_PATH` 파일 방식만 허용한다. 안전한 고정 문구와 제한된 type·ObjectId 라우팅을 사용하며, master 전용 테스트 API는 임의 제목·본문을 받지 않는다.
- 현재 실사용자가 없어 기존 iOS APNs 토큰을 이전할 대상이 없으므로 DeviceToken에 transport 필드를 추가하거나 데이터 마이그레이션을 만들지 않는다. 기존 platform과 전역 token unique, 사용자별 다중 토큰 구조를 유지한다.
- 직접 APNs 발송 코드는 sender 호출에서 제외하되, 운영 FCM 설정으로 실제 iPhone 수신을 확인하기 전까지 `apns.js`와 `APNS_*` 환경 검증을 보존한다. 포그라운드·백그라운드·종료 수신과 토큰 갱신·클릭·OFF·로그아웃을 실기기에서 확인한 뒤 제거한다.
- 이번 전환에서 Web Push 클라이언트 흐름은 변경 범위에서 제외하고 기존 등록·해제 동작을 유지한다. 서버의 기존 Web FCM 발송 호환만 함께 유지한다.

### 이유

- Android와 iOS의 서버 발송 경로를 하나로 줄이면서도 검증 전 APNs 복구 수단을 보존하고, 실사용 데이터가 없는 단계에서 불필요한 스키마와 마이그레이션을 만들지 않기 위함이다.

### 다음에 이어갈 내용

- 운영 Firebase·Apple Push 설정이 반영된 Android/iOS 실기기에서 수신 상태와 라우팅·해제 흐름을 검증한다.
- iOS FCM 검증이 완료되면 미사용 직접 APNs 코드, 관련 테스트와 `APNS_*` 환경 검증을 제거한다.

## 2026-08-13 — 모바일 런타임 권한 최소화

### 논의

- 네이티브 푸시는 인증된 사용자의 알림 설정이 켜졌을 때만 권한을 확인·요청해야 하며, 거부된 권한을 앱 실행 때마다 다시 요청해서는 안 된다.
- 연락처 지인 제외는 사용자가 기능을 켠 때만 연락처 권한을 요청해야 하지만, 기존 구현은 권한 확인 API 오류를 삼킨 뒤 연락처 조회를 계속할 수 있었다.
- 현재 좌표 기반 위치 기능, 네이티브 카메라·사진·마이크 기능은 제공하지 않는다.

### 결정

- 푸시는 `prompt`와 `prompt-with-rationale` 상태에서만 운영체제 권한을 요청하고, 동시 초기화는 한 번으로 합치며 알림 OFF 뒤 ON에서는 권한 상태를 다시 확인한다.
- 연락처는 지인 제외를 켤 때 권한을 확인하고 요청 가능한 상태에서만 요청하며, 최종 승인된 경우에만 전화번호를 읽어 해시 처리한다. 거부 시 앱 설정에서 허용하도록 안내한다.
- iOS에는 연락처 사용 목적 문구를 추가하고, Android에는 연락처 읽기·쓰기와 알림·진동 권한만 유지한다. 현재 사용하지 않는 Android 위치 권한 선언과 위치 일괄요청 유틸은 제거한다.
- 사진 선택은 WebView 파일 선택기를 계속 사용하므로 네이티브 카메라·사진 권한을 추가하지 않고, 마이크 권한도 추가하지 않는다.

### 이유

- 실제 기능을 시작한 시점에 필요한 권한만 요청하고, 반복 팝업과 승인 전 개인정보 접근을 방지하기 위함이다.

### 다음에 이어갈 내용

- 실제 Android/iOS 기기에서 최초 허용·거부·앱 설정 변경·알림 OFF→ON·연락처 지인 제외 흐름을 확인한다.

## 2026-08-13 — 공개 법적 문서와 앱 표시 출처 통합

### 논의

- 앱의 법적 안내 본문이 앱 번들 문서와 관리자가 등록한 본문으로 나뉘어, 공개 URL과 앱 화면의 내용이 달라질 수 있었다.
- 스토어 심사와 로그인 전 접근을 위해 개인정보 처리방침, 이용약관, 아동 안전 기준, 계정 삭제 안내에 고정된 공개 URL이 필요했다.

### 결정

- 이용자에게 보이는 법적 본문은 `https://tazocode-dev.github.io/tazocode-legal/tzchat/` 아래 4개 HTML만 사용하고 앱 내부 iframe으로 표시한다.
- 목록은 공개 문서 4종만 노출하되 기존 12개 slug는 해당 문서와 세부 anchor로 연결해 기존 링크와 동의 흐름을 유지한다.
- 기존 Terms API와 관리자 화면은 본문 게시가 아니라 동의 버전, 문서 ID, 필수·선택 상태와 사용자 동의 기록을 관리하는 용도로 유지한다.
- 설정의 아동 안전 화면과 계정 삭제 안내도 동일한 공개 문서 라우트로 통합한다.

### 이유

- 스토어와 웹에 공개된 정책, 앱에서 읽는 정책을 한 출처로 맞추면서 기존 가입 동의 이력과 재동의 판정을 유지하기 위함이다.

### 다음에 이어갈 내용

- 4개 HTML을 GitHub Pages 목표 경로에 업로드한 뒤 실제 응답과 iframe 표시를 웹·Android·iOS에서 확인한다.
- 운영 동의 메타데이터의 버전·필수 여부가 공개 문서 시행일 및 내용과 일치하는지 검토한다.

## 2026-08-13 — 앱 내 법적 안내 기본본과 서버 문서 우선 정책

### 논의

- 설정의 약관 및 법적 안내 목록은 고정되어 있었지만 상세 본문은 서버 DB의 활성 문서만 조회해, DB가 비었거나 통신 오류가 있으면 빈 화면·오류 화면만 표시됐다.
- 기존 공개 목록에는 현재 제공하지 않는 위치기반서비스 약관과 결제·환불 정책이 포함됐고, 과거 초안에는 현재 확정할 수 없는 사업자 상세정보와 오래된 인증 방식이 남아 있었다.

### 결정

- 서버 DB의 활성 문서가 있으면 제목·본문·버전과 동의 기록에서 계속 최우선으로 사용하며 관리자 게시 내용을 앱 기본본으로 덮어쓰지 않는다.
- 서버 활성 문서가 없거나 본문이 비었거나 조회에 실패한 경우에만 현재 무료 서비스 기준의 앱 내 기본 고지본을 표시한다. 기본본은 빈 화면 방지와 정보 제공 목적이며 동의 버튼·버전 기록의 근거로 사용하지 않는다.
- 공개 목록은 이용약관, 개인정보 처리·수집 안내, 실제 프로필에서 처리하는 민감 가능 정보의 선택 안내, 커뮤니티 안전, 신고·차단, 19세 이상·청소년 보호, 삭제, 연락처 지인 제외, 알림·쿠키·SDK, 제3자 제공 현황, 국외 처리로 한정한다. 현재 미제공인 결제·환불과 좌표 기반 위치서비스 문서는 제공 중인 정책처럼 노출하지 않는다.
- 기본본의 서비스는 `TZChat (앱 표시명: 손끝)`, 운영자는 `tazocode`, 문의는 `tazocode@gmail.com`으로 표기한다. 대표자·주소·전화·사업자번호와 Firebase 처리국가·외부 발송사업자 등 확인되지 않은 값은 확정하지 않는다.

### 이유

- 네트워크·DB 상태와 무관하게 이용자가 앱 안에서 핵심 안전·개인정보 안내를 확인하게 하면서, 법적 효력이 있는 관리자 게시본과 동의 이력을 임의의 앱 번들 문서로 대체하지 않기 위함이다.

### 다음에 이어갈 내용

- 운영자 법적 표시사항, 시행일, Firebase·Apple 처리국가와 계약 주체, 이메일·문자 최종 발송사업자, 업로드 파일·연관 데이터 삭제 범위를 확인하고 전문가 검토 후 서버 활성 문서로 게시한다.
- Apple UGC 심사에서 요구하는 부적절한 콘텐츠 게시 방지 필터의 실제 구현과 신고 처리 응답 절차를 별도 점검한다.

## 2026-08-12 — 사용자별 앱 내 알림과 네이티브 푸시 정비

### 논의

- 채팅 `readBy`와 친구 범주별 변경·확인 시각으로 앱 내 NEW 상태는 유지되고 있었지만, 네이티브 앱은 Web Push 초기화만 호출하여 Android/iOS 토큰을 서버에 등록하지 못했다.
- Android의 수동 FCM 서비스와 채널 설정이 공식 Capacitor 흐름과 중복·불일치했고, iOS는 APNs 등록 delegate와 Push entitlement 및 플랫폼별 전송 경로가 없었다.

### 결정

- 앱 내 채팅 미읽음과 친구 신청·스피드 결과·친구·차단 NEW는 기존 사용자별 서버 상태를 기준으로 유지하고, 소켓과 포그라운드 푸시는 상태 재조회 신호만 보낸다. 탭 열람 시 해당 친구 범주만 확인 처리한다.
- Android/iOS 네이티브 앱은 공식 Capacitor Push Notifications 플러그인으로 권한 요청, 토큰 등록, 포그라운드 표시, 알림 탭 라우팅을 처리한다. 별도 Android FCM 수신 서비스는 제거해 중복 표시를 방지한다.
- Android/Web FCM registration token은 Firebase Admin으로, iOS APNs device token은 APNs HTTP/2로 분리 발송한다. iOS APNs 자격증명이 없으면 발송 준비 완료로 간주하지 않는다.
- Android 채널은 `tzchat_alerts_v2`로 고정해 high 중요도, 기본 소리, 진동, private 잠금화면 표시와 전용 흰색 small icon을 사용한다. iOS는 alert·sound·badge 권한 및 payload를 사용한다.
- 잠금화면에는 실제 채팅 본문·상대 닉네임을 넣지 않고 일반화된 문구만 표시하며, 토큰 해제는 해당 로그인 사용자가 소유한 토큰에만 허용한다.
- 알림 설정 저장 성공 뒤 Web·Native 토큰 상태를 함께 맞추고, 로그아웃·인증 만료 시 두 종류의 로컬 구독을 정리한다. 채팅 딥링크는 실제 MongoDB ObjectId 형식만 허용하고 APNs 요청은 제한 시간 안에 종료한다.

### 다음에 이어갈 내용

- 운영 FCM 서비스 계정과 APNs 키·프로비저닝을 설정한 실제 Android/iOS 기기에서 포그라운드·백그라운드·종료 수신, 소리·진동·badge, 채팅·친구 탭 이동을 두 계정으로 확인한다.

## 2026-08-12 — 로그인 전 전화번호 변경 인증

### 논의

- 휴대전화 번호가 바뀐 사용자가 기존 번호로 로그인할 수 없는 상황에서도 로그인 화면에서 번호를 복구할 수 있어야 했다.
- 설정의 기존 번호 변경은 로그인 세션에 연결된 사용자 ID를 기준으로 현재 이메일과 새 번호를 이중 인증하므로, 공개 화면에 그대로 노출하지 않는 별도 흐름이 필요했다.

### 결정

- 로그인 화면 하단에 `전화번호 변경 인증` 진입점을 제공하고 기존 전화번호, 새 전화번호, 기존 등록 이메일을 입력받는다.
- 서버는 기존 전화 로그인 식별자와 기존 이메일이 같은 계정에 일치하고 전화번호·이메일이 모두 인증된 상태인지 확인한다. 기존 이메일 인증번호를 먼저 확인한 경우에만 새 번호 문자를 발송하되 이메일 코드는 이 단계에서 소비하지 않고, 최종 변경에서 이메일·문자 코드를 함께 소비한다. 두 인증번호가 모두 맞고 새 번호가 다른 계정의 로그인 식별자가 아닐 때만 `phone`과 `loginPhone`을 함께 변경한다.
- 공개 인증번호 요청은 계정 불일치, 인증 미완료, 새 번호 충돌, 재발송·공급자 상태를 응답으로 구분하지 않고 동일한 접수 응답을 즉시 반환한다. 로그인용 심사·고정번호 예외는 번호 변경 인증에 적용하지 않으며, 인증번호는 별도 목적과 사용자·수신처에 묶어 1회 사용 처리한다.

### 이유

- 로그인 세션이 없는 사용자의 복구 수단을 제공하면서 기존 번호와 이메일을 아는 것만으로 번호를 탈취하거나 공개 API로 계정 존재 여부를 확인하는 위험을 낮추기 위함이다.

### 다음에 이어갈 내용

- TZPhone 운영 자격증명 설정 후 수신 가능한 실제 이메일·기존 번호·새 번호로 로그인 전 변경과 새 번호 로그인을 통합 확인한다.

## 2026-08-12 — 채팅 목록 삭제의 사용자별 숨김 처리

### 논의

- 대화 참여자 한 명이 채팅방을 삭제하면 상대방의 대화와 원본 메시지까지 함께 삭제되어, 상대방의 기록 보존과 분쟁·신고 증거 확보를 방해할 수 있었다.
- 삭제는 상대방에게 노출되지 않고, 삭제한 사용자에게는 이후 다시 대화가 시작된 시점부터만 보이는 동작이 필요했다.

### 결정

- 채팅방 `DELETE` 요청은 방과 메시지를 하드 삭제하지 않고, 요청 사용자의 숨김 시각을 채팅방에 기록한다.
- 삭제한 사용자의 목록·상세·미읽음·읽음 처리에서는 숨김 시각 이후의 메시지만 사용한다. 새 메시지가 생기면 그 시점부터 채팅방을 다시 표시한다.
- 숨김 소켓 알림은 요청 사용자에게만 보내고 상대방에게는 삭제 사실을 알리지 않는다.

### 이유

- 한 사용자의 목록 정리가 상대방의 기록을 훼손하지 않게 하고, 기존 채팅방을 재사용하면서도 사용자별로 보이는 대화 경계를 분리하기 위함이다.

### 다음에 이어갈 내용

- 서로 다른 두 계정으로 한쪽 삭제 후 상대방 기록 유지, 삭제 사실 미노출, 새 메시지 이후 재노출을 실제 브라우저·앱에서 확인한다.

## 2026-08-10 — 사용자별 읽음 상태와 변경 종류별 알림 표시

### 논의

- 송신자가 자신이 보낸 채팅을 확인했을 때 수신자의 안 읽음 표시까지 사라지는 현상이 있었고, 친구 하단 알림만으로는 받은 신청·스피드 결과·친구·차단 중 무엇이 변경되었는지 알기 어려웠다.
- 기존 친구 표시는 현재 받은 신청의 존재 여부만 사용해 사용자가 실제로 확인했는지와 앱 재실행 이후 상태를 구분하지 못했다.

### 결정

- 채팅 미읽음은 메시지별 `readBy`와 로그인 사용자 ID로만 계산하고, 읽음 뒤 배지 갱신 이벤트도 실제 읽은 사용자에게만 보낸다. 메시지·읽음의 클라이언트 중복 소켓 발행은 제거한다.
- 친구 관련 알림은 사용자별 `변경 시각`과 `확인 시각`을 서버에 저장하며 받은 신청, 스피드 결과, 친구리스트, 차단리스트를 독립된 범주로 관리한다.
- 하단 친구 배지는 새 변경이 있는 범주 수, 하단 채팅과 채팅방 목록은 안 읽은 메시지 수를 표시한다. 친구 상단 탭에는 범주별 `N` 표시를 제공하고 해당 탭을 누르면 그 범주만 확인 처리한다.
- 개인 알림 소켓 방은 클라이언트 캐시 ID가 아니라 서버 인증 사용자만 참가할 수 있고, 채팅방 소켓 참가도 실제 참여자에게만 허용한다.

### 이유

- 다른 사용자의 읽음이나 오래된 계정 캐시가 내 알림 상태에 영향을 주지 않게 하고, 사용자가 변경 위치를 바로 찾으면서도 앱 재실행 뒤 NEW 상태를 유지하기 위함이다.

### 다음에 이어갈 내용

- 서로 다른 두 계정과 실제 브라우저·앱에서 송수신 읽음 분리, 범주별 NEW 생성·해제, 계정 전환 뒤 소켓 알림 격리를 확인한다.

## 2026-08-09 — PortOne·PASS 본인확인 기능 제거

### 논의

- 기본 로그인을 전화번호 문자 인증으로 전환한 뒤에도 과거 PortOne·다날 기반 PASS 본인확인 화면, 콜백 API, 모델, 환경설정과 관련 문구가 저장소에 남아 있었다.
- 별도 실명·통신사 본인확인을 운영하지 않고 전화번호 문자 인증 흐름만 유지하기로 했다.

### 결정

- PortOne·다날·PASS 본인확인 연동과 전용 전화번호 갱신 흐름을 프론트엔드·백엔드·네이티브 앱 설정에서 제거한다.
- 관련 SDK와 환경변수, CI·DI 저장 구조, 공급자 콜백, 디버그 API 및 실제 서비스에 맞지 않는 관련 약관 문구를 제거한다.
- 기본 전화번호 문자 인증 가입·로그인과 로그인 후 문자 기반 전화번호 변경 기능은 유지한다.

### 이유

- 사용하지 않는 외부 본인확인 경로와 민감 식별정보 구조를 남기지 않고, 인증 구조를 실제 운영 방식에 맞춰 단순화하기 위함이다.

## 2026-08-09 — 기본 로그인을 전화번호 문자 인증으로 전환

### 논의

- 이메일 주소 입력·메일 인증 방식 대신, `tzrank`와 같이 로그인 배경 화면에서 전화번호를 입력하고 문자 인증번호로 가입·로그인하는 흐름이 필요했다.
- 일반 문자 인증 관리자, 고정번호 관리자·스토어 검수 계정, 여러 심사용 테스트 번호를 개발·운영 환경 파일에서 직접 관리해야 했다.
- TZChat의 기존 전화번호는 중복 허용 정책이어서 그대로 로그인 식별자로 사용하면 다른 계정으로 연결될 위험이 있었다.

### 결정

- 2026-07-29의 이메일 기본 로그인 결정을 변경해 전화번호 문자 인증을 기본 가입·로그인 방식으로 사용한다. 기존 이메일 로그인 API는 기존 계정 호환용으로 남기되 기본 화면에서는 제거한다.
- `tzrank`의 로그인 전용 인증 레코드와 1회 소비 흐름을 참고하되, TZChat에서는 기존 프로필 전화번호를 보존하고 별도의 고유 전화 로그인 식별자를 사용한다. 기존 전화번호가 여러 계정에 중복되면 자동으로 하나를 선택하지 않는다.
- 심사용 공통번호 목록, 개별 고정 로그인 번호, 전화 계정별 `master`/`user` 권한을 `.env.development`, `.env.production`에서 관리한다. 일반 인증 관리자는 고정번호 목록에 넣지 않아 실제 문자를 받게 한다.
- TZPhone API 키는 사용자가 직접 입력할 수 있도록 개발·운영 환경 파일에서 비워 둔다.

### 이유

- 모바일 사용자가 이메일 수신함으로 이동하지 않고 전화번호 소유 확인만으로 진입하게 하면서, 기존 중복 전화 데이터로 인한 계정 오연결과 코드 하드코딩을 방지하기 위함이다.

### 다음에 이어갈 내용

- TZPhone 키 입력 후 실제 번호의 문자 수신, 기존 전화번호 계정 연결, 신규 가입·온보딩, 관리자 화면 진입을 실제 환경에서 확인한다.

## 2026-08-07 — 이메일 테스트 계정과 관리자 권한의 환경별 관리

### 논의

- 일반 이메일 인증을 유지할 관리자 계정, 고정번호로 로그인할 별도 관리자·소셜 로그인 검수 계정, 여러 일반 테스트 계정을 개발·운영 환경별로 쉽게 관리할 필요가 있었다.
- 계정과 인증번호가 정책 코드에 하드코딩되어 있어 변경할 때마다 코드 수정과 배포 검증이 필요한 구조를 개선했다.

### 결정

- 2026-08-03의 코드 하드코딩 계정 정책을 변경해, 로그인 전용 고정번호 목록과 계정별 `master`/`user` 권한 보정 목록을 백엔드의 `.env.development`, `.env.production`에서 각각 관리한다.
- 일반 이메일 인증 관리자 계정은 권한 목록에만 두어 실제 인증 메일을 받게 하고, 고정번호 목록의 계정만 로그인 메일 발송을 생략한다.
- 고정번호 예외는 로그인에만 적용하고 이메일·전화번호 변경 인증에는 적용하지 않는다. 목록의 형식 오류와 중복은 서버 시작 단계에서 차단한다.

### 이유

- 1인 운영자가 계정 추가·삭제·번호·권한을 환경 파일에서 직접 관리하면서도, 일반 인증과 로그인 전용 우회의 경계를 유지하기 위함이다.

## 2026-08-03 — 관리자 테스트·스토어 심사용 로그인 계정 추가

### 논의

- 로컬 웹, 서버 웹, Android/iOS 앱에서 실제 이메일 발송 없이 로그인할 수 있는 관리자 권한 테스트 계정과 스토어 심사용 일반 계정이 각각 필요했다.
- 스토어 심사용 계정이 기존 운영 DB 상태 때문에 관리자 권한을 갖지 않도록 권한 보정 기준도 함께 필요했다.

### 결정

- 기존 `test` 계열 5개 계정을 심사용 이메일 고정 인증 목록에서 제거하고, 지정된 두 계정만 각 계정에 서버에 설정된 서로 다른 고정 인증번호를 사용한다.
- 이 예외는 실행 환경과 무관하게 로그인 요청에만 적용하며 실제 이메일을 발송하지 않는다. 이메일·전화번호 변경 인증에는 적용하지 않는다.
- 관리자 테스트 계정은 `master`, 스토어 심사용 일반 계정은 `user`로 신규 생성하며, 기존 계정이나 열린 세션에서도 서버가 해당 권한으로 보정한다.
- 기존 `test@tazocode.com`의 자동 관리자 권한은 제거하고, 기존 DB 계정이나 열린 세션에서도 일반 권한으로 보정한다. 메일 로그인과 별도 정책인 기존 스피드 매칭 시간 예외는 이번 범위에서 유지한다.

### 이유

- 모든 클라이언트가 동일한 백엔드 정책을 사용하게 해 환경별 설정 차이를 없애고, 스토어 심사 계정에 관리자 기능이 노출되는 것을 방지하기 위함이다.

## 2026-08-01 — 앱 전역 가독성·디자인 체계 정리

### 논의

- 수작업으로 작성된 과거 다크 화면과 현재 밝은 전역 테마가 함께 적용되면서 일부 카드·버튼에서 배경과 글자색이 충돌하고 작은 글자의 대비가 부족했다.
- 기존 화면 형태를 반드시 유지하기보다 앱 전체의 일관성과 가독성을 우선해 수정할 권한을 사용자에게 확인받았다.

### 결정

- 앱의 기본 시각 방향은 밝고 따뜻한 뉴트럴 배경, 흰색 카드, 짙은 브라운 텍스트와 브라스 포인트로 통일한다.
- 주요 텍스트, 보조 텍스트, 포인트 링크, 기본·위험 버튼은 작은 글자에서도 읽을 수 있도록 명도 대비를 높이고 공통 토큰을 사용한다.
- 공통 모바일 유틸리티의 검정·흰색 하드코딩을 테마 토큰으로 바꾸고, 로그인·매칭·채팅·관리자처럼 이미 새 체계에 맞는 화면은 구조를 유지한다.
- 밝은 전역 카드 위에 흰 글자가 남던 일반·스피드 상대 프로필과 다크 스타일이 남은 공지·비밀번호 변경·회원탈퇴 화면은 밝은 카드·입력·버튼 체계로 전환한다.

### 이유

- 화면마다 다른 색상 규칙을 유지하는 비용을 줄이고, 배경 강제 변경 뒤 글자가 보이지 않는 회귀를 공통 토큰 수준에서 방지하기 위함이다.
- 1인 운영에서 새 기능 화면도 같은 토큰만 사용해 일관된 품질을 유지할 수 있게 하기 위함이다.

### 다음에 이어갈 내용

- 실제 브라우저 또는 Android/iOS 앱에서 로그인, 상대 프로필, 공지, 비밀번호 변경, 회원탈퇴, 관리자 화면을 모바일 폭과 넓은 화면으로 확인한다.

## 2026-08-01 — 관리자 하단 메뉴 이동과 운영 화면 재구성

### 논의

- 프로필 카드 안의 관리자 버튼보다 주요 화면과 같은 하단 메뉴에서 관리자 화면에 접근하는 구성이 필요했다.
- 기존 관리자 대시보드는 20개의 번호 메뉴를 노출했지만 다수는 준비 중이거나 등록되지 않은 API를 호출해 실제 운영 기능과 맞지 않았다.

### 결정

- 같은 날의 “프로필 카드 오른쪽에 관리자 버튼을 표시한다”는 결정을 변경한다.
- `role=master` 계정에만 하단 메뉴의 `프로필` 오른쪽에 `관리자` 탭을 표시하고 일반 계정은 기존 4개 탭을 유지한다.
- 관리자 대시보드는 API 서버·DB·현재 접속·운영 기록 현황과 회원 확인, 공지 관리, 약관·정책, 베타회원 전환만 노출한다.
- 회원 목록은 master 전용 검색·페이지 API로 조회하고, 공지 관리는 비공개 문서를 포함한 master 전용 목록과 작성·수정·공개 전환·삭제 흐름을 사용한다.
- 기존 번호 관리자 화면 파일은 삭제하지 않되 메뉴에서는 제거하고, 기존 번호 주소는 새 기능 주소 또는 관리자 홈으로 연결한다.

### 이유

- 실제로 동작하는 운영 기능만 보여 주어 1인 운영에서 빈 화면과 잘못된 API 연결로 인한 혼란을 줄이고, 관리자 권한이 없는 사용자에게 진입점과 관리 데이터를 노출하지 않기 위함이다.

### 다음에 이어갈 내용

- 실제 브라우저 또는 앱에서 master·일반 계정 각각의 하단 메뉴 노출과 공지 작성·수정 흐름을 확인한다.

## 2026-08-01 — master 계정의 프로필 관리자 진입 표시

### 논의

- `test@tazocode.com`의 master 권한은 서버에 반영되어 있지만 프로필 화면에서 관리자 페이지로 바로 진입할 수 있는 표시가 필요했다.
- 프론트엔드에는 이미 `/home/admin` 대시보드와 master 전용 하위 라우트가 구성되어 있었다.

### 결정

- 기존 관리자 대시보드와 라우트 구조를 재사용한다.
- 내 프로필 카드 오른쪽 상단에 `role=master`인 계정에만 방패 아이콘과 `관리자` 버튼을 표시하고, 누르면 `/home/admin`으로 이동한다.
- 일반 계정에는 관리자 버튼을 표시하지 않고 기존 설정 버튼만 유지한다.

### 이유

- 이미 구성된 관리자 화면을 중복 구현하지 않고, 서버에서 확인된 권한으로만 진입점을 노출하기 위함이다.

## 2026-08-01 — 테스트 이메일 로그인 고정 인증 범위 변경

### 논의

- 스피드 매칭·관리자 흐름에 사용하는 테스트 계정은 인증 메일 수신 없이 반복 로그인할 수 있어야 했다.
- 기존 고정 인증 허용 목록에는 현재 테스트 범위에 포함되지 않는 계정도 남아 있었다.

### 결정

- 2026-07-29의 `test1`~`test6` 테스트 이메일 로그인 예외 범위를 변경한다.
- `test@tazocode.com`, `test1@tazocode.com`~`test4@tazocode.com` 다섯 계정에서 로그인 인증번호를 요청하면 실제 메일을 발송하지 않고 서버에 설정된 고정 테스트 인증번호를 사용한다.
- `test5@tazocode.com`, `test6@tazocode.com`은 고정 인증 예외에서 제외하고 일반 이메일 로그인 발송 정책을 적용한다.
- 이 예외는 로그인에만 적용하고 계정 이메일·전화번호 변경 인증에는 적용하지 않는다.

### 이유

- 테스트 계정의 반복 로그인은 간단히 유지하면서 고정 인증 예외 범위를 현재 사용하는 계정으로 최소화하기 위함이다.

## 2026-08-01 — 현재 무료 서비스 기준 법률·동의 문서 초안

### 논의

- 기존 프론트 정적 약관에는 현재 사용하지 않는 AWS, AI 자동검수, PASS 필수 본인확인, 유료 구독·결제, 상대 위치 표시가 포함되어 있었고 제3자 제공·국외 이전 동의를 가입 필수로 둘 수 있는 구조였다.
- 실제 구현에서는 이메일·선택적 전화번호 인증, 프로필·매칭·친구·채팅, 연락처 해시 기반 지인 제외, Firebase 푸시·Android Analytics, 14일 탈퇴 유예와 1년 메시지 TTL을 처리한다.

### 결정

- 프로젝트 루트 `legal/`에 현재 무료 서비스 기준의 이용약관, 개인정보 처리방침, 필수 개인정보 동의, 민감정보 동의, 연락처·위치·마케팅 선택 동의, Firebase 국외 이전, 커뮤니티·신고·청소년·삭제·추적기술 정책을 각각 독립 HTML 초안으로 관리한다.
- 현재 상시 제3자 제공은 없는 것으로 문서화하고 빈 제3자 제공 동의를 가입 필수로 사용하지 않는다.
- 좌표 기반 위치 서비스는 현재 제공하지 않는다고 명시하고, 위치 약관은 실제 기능 출시와 사업자 요건 확인 후 적용하는 초안으로 둔다.
- 유료 멤버십·결제는 현재 비활성 범위이므로 환불·구독 문서를 공개 초안에 포함하지 않고 유료화 전에 별도로 제정한다.
- 시행일, Firebase 처리국가, 실제 이메일·문자 최종 발송사업자처럼 운영 확인이 필요한 사항은 확정한 것처럼 쓰지 않고 게시 전 내부 확인표에서 관리한다.

### 이유

- 법률 문구와 실제 수집·이용·보관·삭제 동작을 맞추고, 필수 동의와 선택 기능 동의를 분리해 선택 미동의가 기본 서비스 이용을 막지 않게 하기 위함이다.

### 다음에 이어갈 내용

- 사업자·수탁자·국외 이전 세부정보와 시행일을 확정하고 전문가 검토 후 운영 DB의 새 활성 버전과 앱 동의 화면에 반영한다.
- 동의 전 Firebase Analytics 비활성화, 미사용 위치 권한 자동 요청 제거, 탈퇴 시 업로드 파일과 연관 데이터 삭제를 구현·검증한다.

## 2026-08-01 — 테스트 계정 master 권한·스피드 매칭 상시 시작 정책

### 논의

- 운영 시간 제한 때문에 테스트 계정으로 스피드 매칭 흐름을 수시로 검증하기 어려웠고, 관리자 기능 검증에 사용할 명확한 master 계정이 필요했다.

### 결정

- `test@tazocode.com`은 이메일 로그인 및 기존 세션의 계정 조회·관리자 요청 시 서버 DB의 `role`을 `master`로 보정한다.
- `test@tazocode.com`, `test1@tazocode.com`~`test4@tazocode.com`은 한국시간 운영 시간과 시간대별 1회 제한을 적용하지 않고 언제나 스피드 매칭을 시작할 수 있게 한다.
- 테스트 계정에도 1회 세션의 1시간 지속, 잠시 숨김, 남은 시간 재참여 동작은 일반 계정과 같게 유지하고, 만료 후에는 즉시 다시 시작할 수 있게 한다.
- 일반 계정의 운영 시간·시간대별 1회 정책은 변경하지 않는다.

### 이유

- 실제 세션 타이머와 숨김·재참여 흐름은 유지하면서 테스트 시간 제약만 제거해, 일반 이용자 정책에 영향 없이 반복 검증하기 위함이다.

## 2026-08-01 — 전화번호 인증 발송·화면·테스트 번호 정책 변경

### 논의

- 전화번호 인증번호 입력칸이 문자 요청 전부터 보이고, 로컬 개발의 전역 mock 설정 때문에 실제 번호에도 발송 성공처럼 안내되지만 문자는 오지 않는 문제가 있었다.
- 최초 전화번호 등록과 기존 인증 전화번호 변경에 필요한 소유 확인 범위를 구분했다.

### 결정

- 최초 전화번호 등록은 새 번호로 인증문자를 요청한 뒤 인증번호 입력칸을 표시하고, 문자 인증이 일치하면 등록한다.
- 기존 인증 전화번호 변경은 새 전화번호 문자 인증과 현재 등록 이메일 인증을 모두 완료해야 반영한다.
- 로컬 웹·서버 웹·Android/iOS 앱의 일반 전화번호 인증은 모두 TZChat 백엔드를 거쳐 TZPhone으로 실제 발송하며, 개발 전체 번호에 적용하던 mock·전역 고정번호 정책을 폐기한다.
- `00012341234`, `00011111234`, `00022221234`, `00033331234`, `00044441234`는 모든 실행 환경에서 실제 문자를 보내지 않고 인증번호 `123456`을 사용한다.
- 전화번호 인증번호 입력칸과 번호 변경용 이메일 인증번호 입력칸은 각 발송 요청이 성공한 뒤 표시한다.

### 이유

- 화면의 발송 안내와 실제 동작을 일치시키고, 실제 번호의 발송 실패가 mock 성공 응답에 가려지지 않게 하기 위함이다.
- 최초 등록은 새 번호 소유만 확인하고, 이미 인증된 번호 변경은 열린 로그인 세션만으로 바뀌지 않도록 등록 이메일을 추가 확인하기 위함이다.

### 다음에 이어갈 내용

- TZPhone에 등록된 `tzchat` 앱의 새 API 키를 발급해 백엔드 비밀 환경변수에 설정하고, 수신 가능한 실제 번호로 로컬·운영 문자 수신과 전체 인증 흐름을 확인한다.

## 2026-08-01 — 이메일 인증 발송·심사용 로그인·계정 전환 정책 변경

### 논의

- 기존에는 로컬 개발에서 mock 메일과 전역 고정번호를 사용하고 운영에서만 TZMail을 사용했으나, 이 방식은 일반 로컬 사용자의 실제 발송 실패를 숨기고 앱 심사용 로그인 예외와 일반 인증 정책을 섞는 문제가 있었다.
- 현재 계정의 이메일 변경과 기존의 다른 이메일 계정으로 로그인하는 흐름, 계정 전환 때 서로 다른 계정의 로컬 데이터를 다루는 기준을 함께 구분했다.

### 결정

- 2026-07-30의 “개발은 mock 메일과 개발 고정번호를 사용한다”는 결정을 변경한다. 로컬 웹·서버 웹·Android/iOS 앱의 일반 이메일 인증은 모두 TZChat 백엔드를 거쳐 TZMail로 실제 발송한다.
- 로컬·운영 런타임은 `MAIL_PROVIDER=tzmail`, `EMAIL_CODE_FIXED=false`를 사용한다. 자동화 테스트의 `MAIL_PROVIDER=dev`와 `devCode`는 실제 앱 실행 설정과 분리한다.
- 정확히 지정된 심사용 이메일의 로그인만 고정 인증번호를 사용하고 실제 메일을 발송하지 않는다. 이 예외는 이메일·전화번호 변경 인증에는 적용하지 않는다.
- 다른 계정이 소유한 이메일로 현재 계정 이메일을 변경하는 요청은 계속 차단하되, 사용자가 해당 기존 계정으로 로그인하는 별도 계정 전환 흐름은 제공한다.
- 기존 이메일 로그인은 새 사용자를 만들지 않고 같은 사용자 ID를 사용하며, 다른 기기의 토큰을 무효화하거나 계정별 로컬 데이터를 자동 병합하지 않는다.
- 로컬 백엔드는 원격 TZMail HTTPS API를, TZMail과 같은 서버의 운영 백엔드는 loopback API를 사용한다. TZMail 앱 ID는 운영 등록 상태와 빈 요청 자격증명 검증으로 확인된 값을 사용한다.

### 이유

- 실제 발송 여부와 심사용 예외를 API 응답으로 명확히 구분하고, 로컬·운영·앱에서 같은 일반 사용자 정책을 유지하기 위함이다.
- 이메일 소유권 변경과 계정 로그인을 분리해 기존 계정 접근은 허용하면서 계정 탈취·데이터 혼합 위험은 유지해서 차단하기 위함이다.

### 다음에 이어갈 내용

- 사용자가 허용한 일반 이메일 주소로 실제 수신과 기존 계정 로그인까지 확인한다.
- 운영 서버의 TZPhone 비밀값과 TZChat TLS 인증서를 정리한 뒤 실제 환경 빌드·PM2 reload·HTTPS health·제한된 메일 오류 로그를 확인한다.

## 2026-07-30 — 운영 Nginx의 프로젝트 dist 직접 제공

### 논의

- 운영 프론트 산출물을 별도 공개 폴더에 복사하지 않고, 프로젝트의 `npm run build` 결과를 Nginx가 직접 제공하는 배포 구조를 검토했다.
- 기존 백엔드 하위 Nginx 파일에는 현재 산출물에 없는 정적 법률 문서·App Links 경로가 포함되어 있었고 최상위 `nginx.md`는 설정 원문이 없는 상태였다.

### 결정

- 운영 Nginx 설정의 저장소 기준본은 프로젝트 최상위 `nginx.md`로 관리한다.
- Nginx `root`는 `/home/tazofarm/project/tzchat/tzchatapp/dist`를 직접 사용하며 `/var/www` 등으로 `dist`를 복사하지 않는다.
- `/api/`와 실제 Socket.IO `/socket.io`는 `127.0.0.1:11018`로 전달하고, 백엔드 업로드 파일과 Firebase 서비스워커에 필요한 정적 경로만 별도로 유지한다.
- 운영 서버 설정은 자동 변경하지 않고 사용자가 `nginx.md`를 수동 복사해 반영한다.

### 이유

- 빌드 산출물과 실제 제공 파일의 불일치 및 오래된 복사본 배포를 방지하고, 한 경로만 관리해 1인 운영의 배포 절차를 단순화하기 위함이다.

### 다음에 이어갈 내용

- 서버에서 `nginx.md`를 반영한 뒤 Nginx 구문, `www-data`의 `dist` 읽기 권한, 웹·API health·Socket.IO 연결을 확인한다.

## 2026-07-30 — 개발·운영 환경과 배포 안전 기준

### 논의

- 개발과 운영의 API, CORS, 메일·문자 공급자와 인증번호 정책이 섞이지 않도록 환경 파일 로딩과 배포 경로를 점검했다.
- Capacitor의 `localhost`는 기기 API 주소가 아니라 WebView Origin이라는 점과, PM2 직접 실행 시 ecosystem 설정이 빠질 수 있는 위험을 함께 검토했다.

### 결정

- `NODE_ENV`는 환경 파일보다 먼저 지정하며, 개발은 `.env.development`만 읽고 운영은 `.env` 다음 `.env.production`을 읽는다. 셸과 PM2에서 지정한 값은 파일보다 우선한다.
- 개발은 로컬 웹·API와 mock 메일·문자 공급자만 사용한다. 테스트 이메일 7개의 고정 인증번호는 명시적으로 활성화한 개발·테스트 환경에서만 허용한다.
- 운영은 HTTPS 웹·API, 웹 도메인과 Android `https://localhost`·iOS `capacitor://localhost`의 정확한 CORS 목록, 실제 TZMail·TZPhone 공급자만 허용하며 고정 인증번호 활성화 시 배포 검사를 실패시킨다.
- 이는 2026-07-29의 “테스트 이메일 고정번호가 실행 모드와 무관하게 동작”하던 임시 결정을 폐기하고 운영 인증 우회를 차단하는 변경이다.
- 운영 백엔드는 `ecosystem.config.cjs`로만 PM2 실행·재반영하고, PM2 직접 실행은 기동 검증에서 거부한다.
- Capacitor의 `https://localhost`는 Android WebView Origin으로 유지하되 API는 운영 HTTPS 도메인만 사용하고, Android/iOS 번들에 로컬·사설 API가 포함되면 빌드 검사를 실패시킨다.

### 이유

- 개발 편의 기능과 운영 인증·네트워크 설정의 혼입을 빌드 및 기동 전에 차단하고, 서버 환경 누락으로 인한 장애와 PM2 재시작 반복 가능성을 낮추기 위함이다.

### 다음에 이어갈 내용

- TZPhone 손끝 전용 운영 자격증명을 설정한 뒤 운영 빌드, 서버 PM2 반영, HTTPS health와 실제 이메일·문자 수신을 확인한다.

## 2026-07-30 — 설정 인증정보 변경 정책

### 논의

- 설정에서 전화번호와 이메일 인증 상태를 확인하고 직접 변경할 수 있는 흐름이 필요했다.
- 전화번호 변경은 로그인 상태만으로 허용하지 않고, 계정의 이메일과 새 전화번호를 각각 확인하는 방식을 검토했다.

### 결정

- 설정에 `인증정보` 카테고리를 두고 전화번호는 `번호변경`, 이메일은 미인증 시 `인증하기`, 인증 후 `수정하기`로 제공한다.
- 이메일이 인증된 계정의 이메일 수정은 현재 이메일과 새 이메일로 받은 인증번호를 모두 확인한 뒤 반영한다. 이메일 미인증 계정은 새 이메일 인증으로 최초 인증을 완료한다.
- 전화번호 변경은 이메일 인증 완료 계정만 가능하며, 현재 이메일 인증번호와 새 전화번호 문자 인증번호가 모두 일치해야 서버에서 반영한다.
- 인증번호는 사용자·인증 목적·수신처에 묶어 해시로 저장하며 5분 만료, 재발송 제한, 실패 횟수 제한을 적용한다.
- 문자 발송은 다른 서비스의 키를 재사용하지 않고 TZPhone 손끝 전용 앱 자격증명을 사용한다.

### 이유

- 이메일 로그인 계정의 탈취나 열린 세션만으로 전화번호·로그인 이메일이 바뀌는 위험을 낮추고, 변경되는 새 연락처의 소유도 함께 확인하기 위함이다.

### 다음에 이어갈 내용

- TZPhone 손끝 전용 앱과 API 키를 발급·설정하고 실제 문자 수신과 전화번호 변경 전체 흐름을 확인한다.

## 2026-07-29 — 초기 무료 스피드 매칭 정책 구현

### 반영 결과

- 사용자 화면과 친구 신청 실행 경로에서 포인트 표시·차감·지갑 동기화를 제거했다. 기존 사용자 지갑 데이터와 관련 소스 파일은 복구 가능성을 위해 삭제하지 않았으며 현재 서비스 경로에서는 사용하지 않는다.
- 멤버십·결제 화면 진입은 프로필로 돌리고 백엔드 멤버십·결제 API는 마운트하지 않아 초기 홍보 기간에 유료 기능이 노출되지 않게 했다.
- 스피드 매칭은 한국시간 기준 매일 `13:00~15:00`, `21:00~23:00`에 새로 시작할 수 있고 각 시간대에 한 번, 시작부터 1시간 동안 이용하도록 서버에서 제한한다.
- 잠시 숨기면 후보 목록에서만 제외되고 고정 종료 시각은 유지된다. 남은 시간에는 운영 시작 시간이 지난 뒤에도 다시 참여할 수 있으며, 만료 뒤 같은 시간대 재시작은 차단한다.
- 일반 매칭과 스피드 매칭을 신청 데이터의 `matchType`으로 구분했다. 기존 구분값 없는 신청은 일반 매칭으로 취급하고, 스피드 매칭 결과는 별도 화면에서 신청·수락·취소·채팅 연결을 처리한다.
- 하단 메뉴를 `매칭`, 기존 빠른 만남·Premium 사용자 문구를 `스피드 매칭`으로 바꾸고, 모바일에서는 시작 확인창이 화면 하단 안에 표시되게 했다.

### 확인 결과

- 백엔드 테스트 31개가 통과했으며 스피드 매칭 운영 시간, 종료 경계, 고정 종료 시각 계산을 포함한다.
- 백엔드 운영 빌드 검사와 프론트엔드 타입 검사·운영 빌드·번들 검사가 통과했다.
- 실제 모바일 기기의 화면과 운영 서버·DB를 연결한 시간 경계 및 신청·수락 전체 흐름은 후속 통합 확인이 필요하다.

## 2026-07-29 — 일반 매칭과 스피드 매칭 결과 분리 보완

### 논의

- 앞선 정책 정리에서 일반 매칭과 스피드 매칭의 이용 시간과 비용 정책은 기록했지만, 두 기능의 후보 목록과 신청 결과를 서로 다르게 제공한다는 기존 방향이 누락되었다.
- 현재 프론트엔드는 일반 매칭에서 일반 추천 목록과 일반 프로필을, 기존 긴급모드에서는 현재 활성 사용자 목록과 별도 프로필을 사용한다.
- 그러나 현재 친구 신청 데이터에는 일반·스피드 구분값이 없고, 일반·Premium 신청이 같은 받은 신청 API와 데이터로 합쳐져 있어 결과 분리가 실제로 완성되어 있지 않다.

### 결정

- 일반 매칭과 스피드 매칭은 같은 기능의 유료·무료 차이가 아니라 서로 다른 매칭 경로로 구분한다.
- 일반 매칭은 일반 검색·추천 조건으로 만든 후보와 일반 매칭 신청·결과를 제공한다.
- 스피드 매칭은 같은 시간에 스피드 매칭을 활성화한 사용자만 후보로 제공하고, 신청과 결과에도 스피드 매칭 출처가 유지되게 한다.
- 신청 데이터에 일반 매칭과 스피드 매칭을 구분할 수 있는 값을 저장하고, 받은 신청·보낸 신청 또는 매칭 결과 화면에서도 두 결과를 명확히 구분한다.
- 유료화를 보류했으므로 기존 `Premium` 명칭을 결과 구분값으로 계속 사용하지 않고 `일반 매칭`과 `스피드 매칭`이라는 서비스 용어로 정리한다.

### 다음에 이어갈 내용

- 스피드 매칭 신청이 수락되었을 때 일반 매칭처럼 친구 관계를 만든 뒤 채팅하는지, 별도 스피드 매칭 결과에서 바로 대화를 여는지 최종 연결 방식을 확인한다.
- 현재 주석 처리된 Premium 결과 탭과 중복된 받은 신청 화면을 재사용할지 검토하되, 1인 운영에 맞게 최소한의 구조로 구현한다.

## 2026-07-29 — 초기 무료 운영과 스피드 매칭 정책

### 논의

- 최초에는 문자·기능 사용마다 포인트를 차감하고 스피드 매칭에는 일반보다 두 배의 포인트를 쓰는 소모형 구조를 구상했다.
- 초기에는 유료회원과 이용자가 거의 없으므로 포인트 유지, 유료회원 전용 운영, 성별별 차등 운영보다 회원 확보와 기능 체험을 우선하기로 했다.
- 이용자가 적은 시기에 상시 운영하면 동시 참여자가 흩어질 수 있어, 모두가 같은 시간에 참여하는 시간 집중형 운영을 검토했다.

### 결정

- 초기 홍보 단계에서는 포인트 기반 소모 요소를 제거하고 유료회원 정책은 회원이 충분히 모인 뒤 다시 검토한다.
- 스피드 매칭은 성별이나 회원등급에 차이를 두지 않고 모든 회원에게 무료로 제공한다.
- 평일과 주말을 구분하지 않고 매일 같은 시작 가능 시간인 낮 `13:00~15:00`, 밤 `21:00~23:00`에 운영한다.
- 각 운영 시간대에는 사용자별로 한 번, 시작 시점부터 1시간 동안 스피드 매칭을 이용할 수 있다.
- 처음 시작한 시각을 기준으로 종료 시각을 고정한다. 중간에 끄면 목록에서 잠시 숨겨질 뿐 시간이 초기화되지 않으며, 다시 켜면 남은 시간만 이어서 이용한다.
- 같은 운영 시간대에서 1시간이 끝난 뒤 새 1시간을 다시 시작할 수 없게 서버에서 제한한다.
- 최초 진입은 단순 ON/OFF 스위치보다 `1시간 시작하기`와 확인 화면으로 명확히 안내하고, 이용 중에는 OFF 대신 `잠시 숨기기`로 표현한다.
- 사용자 화면에서는 `빠른 만남` 대신 `스피드 매칭`을 사용하고, 하단 메뉴의 `만남`은 `매칭`으로 바꾼다.
- 스피드 매칭 화면에 매일 낮 `13:00~15:00`, 밤 `21:00~23:00`의 시작 가능 시간을 표시한다.
- 외부 문구에서는 오해 소지가 큰 `급만남` 표현을 사용하지 않는다.

### 이유

- 초기 이용자의 결제 부담을 없애 기능 체험과 회원 확보를 우선한다.
- 스피드 매칭의 차별점은 높은 포인트 비용이 아니라 같은 시간에 즉시 연결되는 집중성과 긴박감으로 만든다.
- 종료 시각을 고정하면 실수로 끈 사용자는 남은 시간을 복구할 수 있고, 반복해서 껐다 켜며 이용 시간을 초기화하는 악용도 막을 수 있다.

### 다음에 이어갈 내용

- 현재 코드에 남은 포인트 차감, 멤버십 제한, 기존 Premium·긴급모드 표현의 실제 영향 범위를 확인한 뒤 최소 변경으로 초기 무료 정책을 반영한다.
- 서버에서 운영 시간대, 시간대별 1회 시작, 고정 종료 시각, 잠시 숨김 상태를 검증하도록 구현한다.
- 하단 메뉴와 스피드 매칭 영역의 명칭·시간 안내·모바일 확인 화면을 반영하고 관련 테스트와 빌드를 실행한다.
- 정식 유료화 시점과 유료 혜택은 실제 회원 수와 이용 데이터를 확인한 뒤 별도로 결정한다.

## 2026-07-29 — 손끝 전용 TZMail 키 연결

### 논의

- `tzadmin/shared_admin/0500_key정리.md`의 TZMail 항목에 추가된 `tzchat` 키를 이메일 인증에 연결했다.
- 키 문서의 `tzchat`은 앱 이름이며 원격 TZMail에 등록된 실제 앱 ID는 별도 값임을 관리자 앱 목록으로 확인했다.

### 결정

- 손끝 이메일 인증은 원격 TZMail 서비스와 손끝 전용 앱 자격증명을 사용한다.
- 자격증명은 Git에서 제외된 백엔드 공통 `.env`에만 저장하고 개발·운영 모드 파일의 빈 값이 이를 덮어쓰지 않게 한다.
- 개발 환경도 실행되지 않는 로컬 TZMail 대신 원격 TZMail 주소를 사용한다.

### 확인 결과

- 원격 TZMail 상태 엔드포인트가 정상 응답했다.
- 실제 메일을 발송하지 않는 빈 본문 요청으로 앱 ID와 API 키 인증 통과를 확인했다.

### 다음에 이어갈 내용

- 사용자가 수신 가능한 일반 이메일로 인증번호 요청과 수신, 로그인까지 실제 통합 검증한다.

## 2026-07-29 — 이메일 인증 테스트 계정 및 발송 실패 처리

### 논의

- `test@tazocode.com`과 일반 이메일에서 인증번호 요청이 429로 보이는 문제를 점검했다.
- 일반 이메일은 TZMail 앱 자격증명이 비어 있어 첫 발송이 실패한 뒤, 실패 레코드가 재발송 제한에 포함되면서 다음 요청이 429로 가려지는 상태였다.

### 결정

- 기존 `test1@tazocode.com`부터 `test6@tazocode.com`까지의 테스트 계정을 유지하고 `test@tazocode.com`을 추가한다. 테스트 인증번호는 기존과 동일한 `123456`을 사용한다.
- 실제 메일 발송에 실패하면 방금 만든 인증 레코드를 제거해 즉시 다시 요청할 수 있게 한다.
- 유효한 인증번호가 이미 발급된 상태에서 재발송 제한 응답을 받으면 로그인 화면을 인증번호 입력 단계로 복구한다.
- 정상 이메일 발송에는 다른 앱의 키를 재사용하지 않고 손끝 전용 TZMail 앱과 자격증명을 사용한다.

### 다음에 이어갈 내용

- TZMail에 손끝 전용 앱을 등록하고 개발·운영 환경의 앱 ID와 API 키를 비밀 환경변수로 설정한다.
- 로컬 개발에서는 TZMail 서버를 실행한 뒤 일반 이메일의 실제 수신까지 통합 검증한다.
- 고정번호 테스트 계정은 현재 실행 모드와 무관하게 동작하므로 공개 출시 전에 모두 제거한다.

## 2026-07-29 — 온보딩 연령 정보 최소 수집

### 논의

- 기존 온보딩은 생년월일 전체를 입력·저장했으나, 가입 단계에서는 출생연도만 확인하는 방향으로 변경했다.
- 출생연도만으로는 생일 기준의 만 나이를 계산할 수 없어 연령 경계 기준을 함께 검토했다.

### 결정

- 신규 이메일 온보딩에서는 생년월일 대신 출생연도만 입력·저장한다.
- 이용 가능 기준은 청소년 보호법의 연령 정의에 맞춰 `현재 연도 - 출생연도 >= 19`로 적용한다.
- 출생연도는 저장 후 사용자가 직접 변경할 수 없으며, 기존 생년월일 저장 계정과 이전 앱의 API 요청은 계속 호환한다.
- 출생연도 확인은 브라우저 기본 팝업 대신 모바일 화면 안의 하단 확인 모달을 사용한다.

### 이유

- 연령 확인에 필요한 정보만 수집해 개인정보 범위를 줄이고, 모바일에서 확인 팝업이 화면 밖으로 벗어나는 문제를 방지하기 위함이다.

### 다음에 이어갈 내용

- 수정된 약관·개인정보 처리방침·청소년 보호정책을 운영 DB의 새 활성 버전으로 검토·게시한다.

## 2026-07-29 — 이메일 기반 성인 데이팅 온보딩

### 논의

- PASS 본인인증은 비용과 가입 이탈 부담이 있고, 이메일·전화번호 인증만으로는 실제 나이나 실명을 확인할 수 없다는 점을 검토했다.
- 이메일 인증 후 약관, 연령, 성별을 어떤 순서와 강제 수준으로 받을지 검토했다.

### 결정

- 일반 회원의 기본 가입·로그인은 이메일 인증번호 방식으로 운영한다.
- 이메일 인증 후 필수 약관 확인, 생년월일을 기준으로 한 만 19세 이상 확인, 성별 선택을 순서대로 완료해야 메인 기능을 이용할 수 있다.
- 생년월일은 저장 후 사용자가 직접 변경할 수 없게 하고, 오입력 정정은 후속 고객지원 절차로 다룬다.
- 선택 약관은 최초에 함께 보여주되 동의하지 않아도 서비스 이용을 막지 않고 거부 상태를 저장한다.
- 가입 중 종료하면 DB에 저장된 마지막 미완료 단계부터 다시 진행한다.
- PASS는 기본 가입 요건에서 제외하고, 전화번호·PASS·소셜 로그인의 추가 범위는 후속으로 검토한다.

### 이유

- 1인 운영 기준의 인증 비용과 구현·지원 복잡도를 낮추면서, 미성년자가 데이팅 기능에 진입하지 못하도록 명시적인 단계를 둔다.
- 선택 동의를 서비스 이용 조건으로 강제하지 않고, 필수 정보와 선택 정보를 분리한다.

### 다음에 이어갈 내용

- 수정된 약관·청소년 보호정책을 운영 DB의 새 활성 버전으로 검토·게시한다.
- 생년월일 오입력에 대한 고객지원 정정 절차와 후속 전화번호·PASS 신뢰 인증 범위를 검토한다.

## 2026-07-29 — 로그인 화면 브랜드 표기 단순화

### 결정

- 사용자용 로그인 화면에는 앱 이름을 `손끝`으로 표기한다.
- 로그인 화면에서 소개 문구와 영문 슬로건을 제거하고 모바일 중심의 단일 폼으로 간결하게 구성한다.
- 이번 변경 범위는 로그인 화면이며, 기존 `TZChat` 표기가 남은 다른 화면과 약관의 일괄 변경은 포함하지 않는다.

### 이유

- 모바일 첫 화면에서 브랜드 설명보다 로그인 행동에 바로 집중할 수 있도록 하기 위함이다.
